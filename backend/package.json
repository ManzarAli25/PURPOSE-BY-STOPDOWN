{
  "name": "mobile-crm-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Mobile CRM",
  "scripts": {
    "start": "uvicorn main:app --reload",
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}